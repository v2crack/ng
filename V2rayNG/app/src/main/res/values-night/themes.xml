<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Night theme: inherit the shared AppThemeBase and only override night-specific items -->
    <style name="AppThemeDayNight" parent="AppThemeBase">
        <!-- Night mode specific overrides -->
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
        <!-- Font is applied at runtime via AppFontHelper (default: Russo One) -->
        <item name="alertDialogTheme">@style/RoundedAlertDialog</item>
    </style>
    <style name="RoundedAlertDialog" parent="Theme.Material3.DayNight.Dialog.Alert">
        <item name="android:windowBackground">@drawable/dialog_rounded_background</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:padding">0dp</item>
        <item name="android:windowMinWidthMajor">85%</item>
        <item name="android:windowMinWidthMinor">85%</item>
    </style>
</resources>
